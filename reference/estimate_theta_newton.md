# Estimate theta using Newton-Raphson with bisection fallback

Estimate theta using Newton-Raphson with bisection fallback

## Usage

``` r
estimate_theta_newton(
  score,
  gamma,
  score_min,
  score_max,
  tol = 1e-07,
  max_iter = 500
)
```
