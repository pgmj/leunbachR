# Estimate theta using bisection method

Estimate theta using bisection method

## Usage

``` r
estimate_theta_bisection(
  score,
  gamma,
  score_min,
  score_max,
  tol = 1e-06,
  max_iter = 100
)
```
