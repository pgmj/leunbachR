# Run bootstrap samples in parallel using mirai

Run bootstrap samples in parallel using mirai

## Usage

``` r
run_bootstrap_parallel(
  nsim,
  boot_seeds,
  boot_data_list,
  n_cores,
  verbose = FALSE
)
```
