% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/equate_direct.R
\name{leunbach_equate}
\alias{leunbach_equate}
\title{Equate Scores Between Tests using Leunbach Model}
\usage{
leunbach_equate(
  fit,
  direction = c("1to2", "2to1"),
  method = c("optimize", "newton"),
  verbose = FALSE
)
}
\arguments{
\item{fit}{A leunbach_ipf object from leunbach_ipf()}

\item{direction}{Direction of equating:  "1to2" (Test1 to Test2) or "2to1" (Test2 to Test1)}

\item{method}{Optimization method for person parameter estimation:
"optimize" (default) uses stats::optimize() with Brent's method,
"newton" uses custom Newton-Raphson with bisection fallback}

\item{verbose}{Print detailed output}
}
\value{
A list of class "leunbach_equating" containing:
\itemize{
\item equating_table: Data frame with original scores, theta, expected equated scores, and rounded scores
\item direction: Direction of equating
\item method: Optimization method used
\item fit: Original leunbach_ipf object
}
}
\description{
Creates equating tables to convert scores from one test to another using
the estimated score parameters from the Leunbach model.
}
\examples{
# Simulate paired test score data
set.seed(123)
n <- 400
theta <- rnorm(n)
test1 <- pmin(pmax(round(3 + 1.5 * theta + rnorm(n, sd = 0.8)), 0), 6)
test2 <- pmin(pmax(round(2.5 + 1.3 * theta + rnorm(n, sd = 0.7)), 0), 5)
fit <- leunbach_ipf(data.frame(test1, test2),
                    max_score1 = 6, max_score2 = 5)

# Equate Test 1 scores onto the Test 2 metric
eq <- leunbach_equate(fit, direction = "1to2")
print(eq)

}
