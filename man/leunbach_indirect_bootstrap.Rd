% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/equate_indirect.R
\name{leunbach_indirect_bootstrap}
\alias{leunbach_indirect_bootstrap}
\title{Bootstrap for Indirect Equating}
\usage{
leunbach_indirect_bootstrap(
  fit_ab,
  fit_bc,
  direction_ab = c("1to2", "2to1"),
  direction_bc = c("1to2", "2to1"),
  nsim = 1000,
  conf_level = 0.95,
  see_type = c("rounded", "expected"),
  method = c("optimize", "newton"),
  parallel = TRUE,
  n_cores = NULL,
  verbose = FALSE,
  seed = NULL
)
}
\arguments{
\item{fit_ab}{A leunbach_ipf object for the A-B equating}

\item{fit_bc}{A leunbach_ipf object for the B-C equating}

\item{direction_ab}{Direction for A-B equating:  "1to2" or "2to1"}

\item{direction_bc}{Direction for B-C equating: "1to2" or "2to1"}

\item{nsim}{Number of bootstrap samples (default: 1000)}

\item{conf_level}{Confidence level for intervals (default: 0.95)}

\item{see_type}{Type of SEE calculation:  "rounded" or "expected"}

\item{method}{Optimization method: "optimize" (default) or "newton"}

\item{parallel}{Use parallel processing if mirai is available (default: TRUE)}

\item{n_cores}{Number of cores for parallel processing}

\item{verbose}{Print progress messages}

\item{seed}{Random seed for reproducibility}
}
\value{
A list of class "leunbach_indirect_bootstrap" containing:
\itemize{
\item indirect_eq:  The observed indirect equating object
\item Bootstrap results and standard errors
\item Bootstrap p-values for LR and Gamma tests for both equatings
}
}
\description{
Performs parametric bootstrapping for indirect equating to assess
standard errors of the indirect equated scores.
}
\examples{
\donttest{
set.seed(123)
n <- 300
theta1 <- rnorm(n)
a <- pmin(pmax(round(3 + 1.5 * theta1 + rnorm(n, sd = 0.8)), 0), 6)
b1 <- pmin(pmax(round(2.5 + 1.3 * theta1 + rnorm(n, sd = 0.7)), 0), 5)
theta2 <- rnorm(n)
b2 <- pmin(pmax(round(2.5 + 1.3 * theta2 + rnorm(n, sd = 0.7)), 0), 5)
cc <- pmin(pmax(round(3 + 1.4 * theta2 + rnorm(n, sd = 0.8)), 0), 6)
fit_ab <- leunbach_ipf(data.frame(a, b1), max_score1 = 6, max_score2 = 5)
fit_bc <- leunbach_ipf(data.frame(b2, cc), max_score1 = 5, max_score2 = 6)

boot <- leunbach_indirect_bootstrap(fit_ab, fit_bc,
                                    direction_ab = "1to2",
                                    direction_bc = "1to2",
                                    nsim = 25, parallel = FALSE,
                                    seed = 1)
print(boot)
}

}
